rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow anyone (public) to view content
      allow read: if true;
      // Only allow logged-in admins to add/edit/delete
      allow write: if request.auth != null;
    }
  }
}
